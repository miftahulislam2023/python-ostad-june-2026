# ∈ 
A = {
    1, 3, 5, 7
}

B = {
    2, 4, 6, 8
}

# union -> A ∪ B = {x: x ∈ A or x ∈  B}
print(A | B)

# intersection -> A ∩ B = {x: x ∈ A and x ∈ B}
print(A & B)

# difference -> A - B = {x: x ∈ A and x does not belong to B}
print(A - B)